---
type Props = {
  thumbnail: string;
  title: string;
};

const { thumbnail, title }: Props = Astro.props;
---

<img
  alt={`${title} thumbnail`}
  class="h-full w-full scale-110 object-cover transition-transform duration-200 group-hover/card:scale-100"
  height="384"
  src={thumbnail}
  width="384"
/>
